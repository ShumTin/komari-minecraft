{
  "name": "komari-minecraft",
  "private": true,
  "version": "1.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "verify": "npm test && npm run build && git diff --check",
    "package:theme": "npm run build && pwsh -NoProfile -File scripts/package-theme.ps1"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "latest",
    "lucide-vue-next": "^0.577.0",
    "uplot": "^1.6.32",
    "vite": "latest",
    "vue": "latest"
  }
}
