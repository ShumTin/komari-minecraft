# Komari Minecraft 开发计划

## 项目定位

这是一个使用 Vue 3 实现的 Komari 浅色主题监控面板。页面通过 Komari RPC API 加载节点、实时指标和 Ping 数据，支持节点筛选、详情查看、主题切换和响应式布局。

## 参考仓库

以下仓库仅用于参考页面结构、视觉细节和数据展示方式，不直接复制实现代码：

- [vaspike/junimo](https://github.com/vaspike/junimo)：整体页面布局、卡片层级和监控信息组织。
- [imengying/Komari-Glass](https://github.com/imengying/Komari-Glass)：Komari 数据展示、节点状态、地区分组和国旗图标。

## 当前技术栈

- Vue 3 + `<script setup>`
- Vite
- JavaScript
- 原生 CSS（按页面区域拆分在 `src/styles/`）
- Komari RPC API
- Node.js 内置测试运行器

暂不引入 Tailwind、状态管理库或 UI 组件库。

## 已完成

### 页面与交互

- [x] 浅色主题监控首页和顶部概览卡片。
- [x] 深色主题切换与响应式布局。
- [x] 主题偏好持久化，支持浅色、深色和系统主题，并在系统主题变化时自动同步。
- [x] 地区分组筛选、节点卡片和完整指标展示。
- [x] 节点详情独立路由 `/instance/:uuid`，支持浏览器前进、后退、刷新和主机切换。
- [x] 详情页系统、网络、负载、延迟趋势和统计时间切换。
- [x] 加载、错误、空状态及手动刷新交互。
- [x] 首页和详情路由初次加载时显示独立的居中 loading 状态，不复用概览卡片占位。
- [x] 实例 favicon、后台入口和开发代理配置。

### 数据与资源

- [x] 接入 Komari 节点、实时记录、Ping 任务、Ping 记录和 Ping 统计 RPC。
- [x] 统一 API 数据映射为页面节点模型。
- [x] 处理节点离线、部分接口失败、流量单位、价格、到期时间和更新时间。
- [x] 本地国旗 SVG 资源集、地区代码解析和图片失败回退。

### 验证

- [x] Komari API 数据映射测试。
- [x] `npm test` 通过。
- [x] `npm run build` 通过。

## 当前阶段：质量与可维护性

### 阶段零：主机详情页动态数据完善（进行中）

- [x] 新增独立历史数据服务，封装统一 records 结构、请求缓存、并发去重和接口降级。
- [x] 历史请求支持 `AbortController`，切换节点或时间范围时取消旧请求。
- [x] 移除峰值速度、连接数/进程数、内核版本、运行时间和厂商信息中的硬编码值。
- [x] 确认 Komari RPC 是否提供内核、厂商、连接数和进程数字段；缺失时显示“暂无数据”。
- [x] API 层保留最近记录列表，支持 CPU、内存、硬盘和网络指标的历史序列。
- [x] 使用真实历史序列生成负载图表，避免固定 SVG 路径。
- [x] 图表绘制前按指标保留极值降采样，并对长时间数据缺口断线。
- [x] 使用 `node.pingLines` 渲染延迟图例、延迟曲线和丢包率。
- [x] 参考 Junimo 拆分请求职责：首页保留卡片所需的 Ping 历史样本，详情页切换到延迟视图后按需加载完整历史。
- [x] 详情页 Ping 请求支持按节点/时间范围取消，避免切换视图后继续占用请求。
- [x] 负载支持“实时 / 1 小时 / 4 小时 / 1 天”，Ping 仅支持“1 小时 / 4 小时 / 1 天”。
- [x] 区分累计流量与时间范围内流量，避免把累计值标记为“今日流量”。
- [ ] 为详情页动态数据、空数据和接口失败场景补充测试。

验收标准：详情页所有指标和曲线均来自当前主机 API 数据；没有可用数据时明确显示空状态，不展示示例数值。

### 阶段一：真实实例联调（已完成）

- [x] 使用实际 Komari 实例验证节点、Ping、流量和到期数据。
- [x] 验证无节点、全离线、部分节点接口失败和 Ping 任务为空等场景。
- [x] 验证自动刷新期间详情页选中节点不会丢失，路由刷新可恢复详情。
- [x] 核对不同 Komari 版本的 RPC 字段和鉴权/跨域配置。

验收标准：在目标 Komari 实例上完成首页、筛选、详情、刷新和异常状态的完整演示。

### 阶段二：生产部署（已完成）

- [x] 确认静态资源、`/instance/:uuid` 深链接和 `/admin` 后台入口的服务器回退规则。
- [x] 配置生产环境同源 API 代理或反向代理，并验证 WebSocket/Origin 行为。
- [x] 补充生产环境配置说明和部署示例。
- [x] 在桌面端、平板和移动端做一次最终布局检查。

验收标准：生产构建部署后，直接访问详情 URL、刷新页面和打开后台入口均正常。

### 阶段三：质量与可维护性（进行中）

- [x] 参考 Junimo 增加批量最新状态适配，首页实时指标优先走批量接口。
- [x] 将批量实时 usage 刷新从 15 秒全量快照中独立出来，支持 2 秒更新与并发保护。
- [x] 将全量节点/Ping 快照刷新间隔调整为 30 秒，降低历史数据重复请求。
- [x] 为 Ping 任务、历史和统计增加 TTL 缓存，避免首页与详情页重复请求。
- [x] 详情页 Ping 仅支持 1 小时、4 小时和 1 天范围，移除实时 Ping 请求。
- [x] RPC2 优先使用 `/api/rpc2` WebSocket 长连接，HTTP POST 作为连接失败时的兼容降级。
- [x] 暗色主题图表使用独立的坐标轴、网格和刻度颜色，切换主题时重建画布避免残留浅色线条。
- [x] 统一图表坐标轴主线、网格线和数据线的层级与粗细，避免起始刻度重叠加粗。
- [x] 优化暗色主题主机切换列表和地区筛选控件的文字、背景与边框对比度。
- [x] Vite 开发代理补充 WebSocket 升级请求的 Origin 转发，避免握手被 Komari 拒绝。
- [x] 已验证 HTTP fallback 可正常加载节点、usage、Ping 和详情数据（诊断参数已移除）。
- [x] WebSocket 不可用时限制 HTTP 实时 fallback 频率，避免每 2 秒重复请求。
- [x] 首页实时指标优先使用 `common:getNodesLatestStatus` 批量请求，并缓存能力探测结果；旧版本自动降级。
- [x] 为首页刷新增加请求去重和重叠保护，避免慢请求导致多轮快照并发。
- [ ] 在目标生产环境确认 `/api/rpc2` 返回 `101 Switching Protocols`，并确认实时消息进入 WebSocket 而非 HTTP。
- [ ] 增加 WebSocket 断线、重连和 HTTP fallback 限流的自动化测试。
- [ ] 补充 API 错误、离线节点、空状态和路由恢复的自动化测试。
- [ ] 为关键组件补充基础交互测试。
- [ ] 清理未使用的 CSS、旧 Mock 文档和过时注释。
- [ ] 增加 `git diff --check` 与构建检查的提交前流程。

验收标准：关键数据映射和核心交互有稳定测试覆盖，构建与检查命令可重复通过。

## 后续候选功能

以下功能不阻塞当前版本发布，按实际使用反馈排期：

- 流量详情视图和历史趋势。
- 节点列表/卡片视图切换。
- 告警列表与状态筛选。
- 数据源配置界面和更细粒度的刷新设置。

## 暂不处理

- 不制作 Komari 设置后台。
- 不引入 Tailwind、状态管理库或大型 UI 组件库。
- 不恢复星露谷主题专属逻辑。
