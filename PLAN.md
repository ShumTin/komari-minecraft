# Komari Minecraft 开发计划

## 项目定位

这是一个使用 Vue 3 从零实现的 Komari 监控面板原型。当前以原仓库浅色主题为视觉参考，页面使用本地 Mock 数据，不直接连接 Komari 服务端。

## 当前技术栈

- Vue 3
- Vite
- JavaScript
- 原生 CSS
- 本地 Mock Service

暂不引入 Tailwind、状态管理库或 UI 组件库。只有当项目规模和需求明确需要时，才评估新增依赖。

## 代码规范

### Vue 组件

- 使用 `<script setup>` 编写组件逻辑。
- 一个组件只负责一个清晰的页面职责。
- 页面组件负责组合和状态协调，展示组件通过 `props` 接收数据。
- 子组件通过事件向上通知用户操作，不直接修改父组件状态。
- 组件名使用 PascalCase，例如 `NodeCard.vue`、`NodeDetails.vue`。
- 事件名使用 kebab-case，例如 `@select-node`、`@close`。
- 避免在模板中编写复杂表达式，复杂逻辑移动到计算属性或方法中。
- 避免在组件内部重复定义相同的展示逻辑，抽取为可复用组件。

### JavaScript

- 使用 `const` 优先，只有确需重新赋值时使用 `let`。
- import 全部放在文件顶部。
- 函数保持单一职责，避免通过布尔 flag 切换多种行为。
- 数据转换、筛选和格式化使用普通确定性代码完成，不交给模型推断。
- 为复杂数据结构使用稳定、清晰的字段命名。
- Mock 数据和 API 数据保持相同的数据结构，避免页面组件感知数据来源。
- 注释优先使用中文，解释原因和约束，不重复描述代码表面行为。

### CSS

- 使用本项目自己的语义类名，不沿用原仓库的类名体系。
- 类名按组件或区域命名，例如 `monitor-app`、`overview-card`、`node-card`、`details-panel`。
- 不使用无语义的颜色或布局类名作为主要选择器。
- 组件样式集中在 `src/styles.css`，保持同一组件的规则相邻。
- 颜色、间距、圆角和边框尽量使用 CSS 自定义属性统一管理。
- 优先使用 Flex 和 Grid，避免通过绝对定位完成主要页面布局。
- 每次样式调整后执行格式化和构建检查。

### Mock 数据

- 原始静态数据放在 `src/mock/data.js`。
- 数据读取、复制和详情组装放在 `src/mock/mockService.js`。
- 页面组件不直接依赖 `data.js`，统一通过 Mock Service 获取数据。
- 详情数据应覆盖卡片未展示但详情页需要的字段，例如操作系统、Ping、丢包率和更新时间。
- Mock 字段命名应尽量贴近未来 Komari API，减少后续适配成本。

### Git 与验证

- 多个相关改动集中完成后再提交，避免为每个细小样式改动单独创建 commit。
- 提交前至少执行：

  ```bash
  npm run build
  git diff --check
  ```

- 不确定是否成功时明确说明，不把部分完成描述为全部完成。
- `PLAN.md` 是开发文档，除非明确要求，不纳入功能提交。

## 当前已完成

- Vue 3 + Vite 基础项目。
- 浅色主题监控首页。
- 顶部概览卡片。
- 地区分组筛选。
- 节点卡片及完整指标区域。
- 节点详情独立页面与浏览器历史导航。
- 本地 Mock Service。
- 浅色/深色主题切换按钮。
- 响应式布局。
- 详情页主机下拉切换、负载/延迟视图和统计时间切换。
- 详情页系统、网络信息卡片与延迟趋势图。

## 接下来目标

### 阶段一：完善 Mock 交互

- [ ] 增加更多节点和地区分组，覆盖在线、警告、离线状态。
- [ ] 让概览卡片数据由节点 Mock 数据计算生成，避免重复写死。
- [ ] 增加 Mock 定时刷新机制，模拟 CPU、内存、流量和在线状态变化。
- [x] 完善节点详情独立页面的指标展示、返回和主机切换交互。
- [ ] 增加空状态和异常状态展示。

验收标准：所有首页数据都来自 Mock Service，筛选、详情和刷新链路可以独立演示。

### 阶段二：页面结构扩展

- [ ] 增加流量详情视图。
- [ ] 增加节点列表视图和卡片视图切换。
- [ ] 增加告警列表视图。
- [ ] 抽取统一的状态徽标、指标条和数据格式化组件。
- [ ] 为主要组件补充基础交互测试。

验收标准：新增页面不复制节点卡片逻辑，公共展示组件可以复用。

### 阶段三：Komari API 适配

- [ ] 确认 Komari 节点、概览、流量和 Ping 接口字段。
- [ ] 新增 API Service，与 Mock Service 保持相同返回结构。
- [ ] 增加请求加载、超时、错误和重试状态。
- [ ] 通过环境变量选择 Mock 或真实 API 数据源。
- [ ] 对操作系统、地区、流量单位和时间字段做统一适配。

验收标准：切换到真实 API 时无需修改页面组件，只替换数据服务配置。

### 阶段四：视觉还原与质量

- [ ] 按参考截图逐项校准尺寸、间距、字体和边框。
- [ ] 统一浅色/深色主题的 CSS Token。
- [ ] 检查桌面端、平板和移动端布局。
- [ ] 清理未使用的 CSS 和历史样式。
- [ ] 增加生产构建和关键交互的自动化检查。

验收标准：页面视觉结构与参考主题一致，响应式布局无明显溢出，构建和检查命令稳定通过。

## 暂不处理

- 不制作 Komari 设置后台。
- 不在当前阶段引入 Tailwind。
- 不恢复星露谷主题专属逻辑。
- 不在没有明确 API 字段前假设真实后端数据结构。
